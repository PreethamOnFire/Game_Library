<Page x:Class="Game_Library.Models.Views.GamePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Game_Library.Models.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GamePage">

    <ScrollViewer>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="250"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Border BorderThickness="8" BorderBrush="#5e43f3" Height="200" Width="200">
                    <Image Source="{Binding SelectedGame.logoPath}" Height="200" Width="200"/>
                </Border>
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <TextBlock Style="{StaticResource gameText}" Text="{Binding SelectedGame.Name}"/>
                    <Button Grid.Row="1" Style ="{StaticResource playButton}" Name="playButton" Click="playButton_Click">
                        <TextBlock Text="PLAY" Style="{StaticResource playButtonText}"/>
                    </Button>
                </Grid>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
